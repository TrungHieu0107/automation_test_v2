name: Mark Task Complete
description: Child test that marks a task as completed

submit:
  - type: click
    selector: '.todo-list li:first-child .toggle'

assertions:
  - type: exists
    selector: '.todo-list li.completed'
    timeout: 3000
